rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the server (Firebase Admin SDK), which
    // bypasses these rules and enforces authorization against the NextAuth
    // session instead. Deny direct client access entirely.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

